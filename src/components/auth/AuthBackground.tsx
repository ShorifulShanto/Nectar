
"use client";

import React from "react";

/**
 * Simplified background for the new split-pane format.
 * The primary visualization is now inside the Split Pane.
 */
export function AuthBackground() {
  return (
    <div className="fixed inset-0 pointer-events-none z-0 bg-[#121212]" />
  );
}
